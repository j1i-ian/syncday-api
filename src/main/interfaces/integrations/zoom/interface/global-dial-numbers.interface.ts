import { NumberType } from '../enum/number-type.enum';

export interface GlobalDialInNumbers {
    city: string;
    country: string;
    country_name: string;
    number: string;
    type: NumberType;
}
