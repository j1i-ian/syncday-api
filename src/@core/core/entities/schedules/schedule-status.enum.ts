export enum ScheduleStatus {
    /**
     * open
     */
    OPENED = 'opened',

    /**
     * schedule is completed
     */
    EXPIRED = 'expired',

    /**
     * schedule is cancelled
     */
    CANCELLED = 'cancelled'
}
